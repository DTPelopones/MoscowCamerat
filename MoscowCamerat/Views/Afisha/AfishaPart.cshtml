@model IEnumerable<MoscowCamerat.Domain.Photo>

@foreach (var photo in Model)
{
    <a href="@photo.path" data-size="@photo.size" data-med="@photo.path" data-med-size="@photo.size" class="crop" style="text-align: center;">
        <img src="@photo.pathm" class="" alt="@photo.alt">
    </a>


}